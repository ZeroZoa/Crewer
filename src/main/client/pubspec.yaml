name: client
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  go_router: ^15.1.2
  google_maps_flutter: ^2.12.2
  shared_preferences: ^2.5.3
  lucide_icons: ^0.257.0
  geolocator: ^14.0.1
  google_maps_flutter_android: ^2.16.1
  stomp_dart_client: ^2.1.3
  intl: ^0.20.2
  table_calendar:
  flutter_secure_storage:
  provider:
  jwt_decode:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/

  fonts:
    - family: CustomFont  # 폰트 패밀리 이름
      fonts:
        - asset: assets/fonts/OtherHand.otf
